import { selectAccountId } from "./selectAccountId";
import { selectAdGroupId } from "./selectAdGroupId";
import { selectCampaignId } from "./selectCampaignId";
import { selectCustomerId } from "./selectCustomerId";
export default {
  selectAccountId,
  selectAdGroupId,
  selectCampaignId,
  selectCustomerId,
};
