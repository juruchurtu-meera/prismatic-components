import { connection } from "./common";
export const selectCategoryInputs = { connection };
export const selectComputerInputs = { connection };
export const selectDepartmentInputs = { connection };
export const selectMobileDeviceInputs = { connection };
export const selectPackageInputs = { connection };
export const selectScriptInputs = { connection };
export const selectWebhookInputs = { connection };
