import { connectionInput } from "./common";
export const selectAccountInputs = {
  xeroConnection: connectionInput,
};
export const selectContactInputs = {
  xeroConnection: connectionInput,
};
export const selectInvoiceInputs = {
  xeroConnection: connectionInput,
};
export const selectItemInputs = {
  xeroConnection: connectionInput,
};
export const selectPaymentInputs = {
  xeroConnection: connectionInput,
};
