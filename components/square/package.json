{
  "name": "@prismatic-io/square",
  "private": true,
  "scripts": {
    "publish": "npm run build && prism components:publish",
    "build": "npm run format && npm run lint && webpack",
    "test": "echo 'no tests'",
    "lint": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.19.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.0.0",
    "copy-webpack-plugin": "13.0.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "ts-loader": "^9.6.1",
    "typescript": "^5.9.3",
    "webpack": "^5.107.2",
    "webpack-cli": "^6.0.1"
  }
}
