import { connection } from "./common";
export const selectCandidateInputs = {
  connection,
};
export const selectJobApplicationInputs = {
  connection,
};
export const selectJobRequisitionInputs = {
  connection,
};
